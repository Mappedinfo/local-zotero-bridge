{
  "name": "local-zotero-bridge",
  "version": "0.2.18",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --experimental-transform-types --test tests/*.test.ts",
    "build": "node scripts/build.mjs",
    "package": "npm run build && node scripts/package-xpi.mjs",
    "safe-install:profile": "npm run package && node scripts/safe-local-install.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3"
  }
}
